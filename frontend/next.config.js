/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['your-image-domain.com'], // add your backend/image domain here if needed
  },
};

module.exports = nextConfig;
